# 按需添加其他依赖

> 导入自 Claude AI · 2026-04-16 · 代码

C++ 新建 Metric 流程

共需改动 5处文件（3新建 + 5修改）：

---

第一步：定义 Proto（simulation/simulation/simulation_proto/）

新建 your_metric_name.proto：

syntax = "proto3";

package xpilot.simulation.proto;

message YourMetricName {

// metric 输入参数（场景配置）

}

message YourMetricNameResult {

// metric 输出字段（每帧记录）

}

修改 scenario_metric.proto（两处）：

import "simulation/simulation_proto/your_metric_name.proto";  // 顶部添加

// oneof metric 中添加（字段编号全局唯一）：

YourMetricName your_metric_name = 305;

修改 frame_passing_result.proto（两处）：

import "simulation/simulation_proto/your_metric_name.proto";  // 顶部添加

// FrameResult 的 oneof result 中添加：

YourMetricNameResult your_metric_name_result = 278;

---

第二步：实现 Handler（post_process/scenario_evaluator/metrics/）

新建 your_metric_name_handler.h：

#ifndef SCENARIO_EVALUATOR_METRICS_YOUR_METRIC_NAME_HANDLER_H_

#define SCENARIO_EVALUATOR_METRICS_YOUR_METRIC_NAME_HANDLER_H_

#include "metric_handler_base.h"

// 按需 include 所需消息类型头文件

namespace xpilot::simulation::scenario_evaluator {

class YourMetricNameHandler : public MetricHandlerBase {

private:

proto::FramePassingResult* frame_passing_result_ = nullptr;

// 内部状态成员变量

void ProcessTopicA(const SomeMsgType& msg, int64_t system_time);

public:

YourMetricNameHandler(const proto::ScenarioMetric& metric,

proto::ScenarioMetricResult* metric_result);

~YourMetricNameHandler();

bool Evaluate(const std::string& topic, int64_t system_time,

int64_t source_timestamp, const msg::BaseMsg* msg) override;

};

}  // namespace xpilot::simulation::scenario_evaluator

#endif

新建 your_metric_name_handler.cpp：

#include "your_metric_name_handler.h"

#include "simulation/sim_common/topics.h"

namespace xpilot::simulation::scenario_evaluator {

YourMetricNameHandler::YourMetricNameHandler(

const proto::ScenarioMetric& metric,

proto::ScenarioMetricResult* metric_result)

: MetricHandlerBase(metric, metric_result) {

// 必须校验 metric 类型

assert(proto::ScenarioMetric::kYourMetricName == metric.metric_case());

frame_passing_result_ = metric_result->mutable_frame_passing_result();

// 从 metric.your_metric_name() 读取配置参数

}

YourMetricNameHandler::~YourMetricNameHandler() {

// 写入最终统计结果

frame_passing_result_->set_num_passed_frames(num_pass_frame_);

frame_passing_result_->set_num_total_frames(num_total_frame_);

const double pass_ratio = num_total_frame_ ?

static_cast<double>(num_pass_frame_) / num_total_frame_ : 1.0;

metric_result()->set_is_passed(pass_ratio >= metric().passed_ratio());

}

bool YourMetricNameHandler::Evaluate(const std::string& topic,

int64_t system_time, int64_t source_timestamp, const msg::BaseMsg* msg) {

assert(topic == topic::kSomeTopicName);

if (topic == topic::kSomeTopicName) {

const SomeMsgType& typed_msg = *static_cast<const SomeMsgType*>(msg);

ProcessTopicA(typed_msg, system_time);

}

return true;

}

// 文件末尾必须调用，实现自注册

REGISTER_METRIC_HANDLER(YourMetricNameHandler);

}  // namespace xpilot::simulation::scenario_evaluator

---

第三步：注册 Topic 订阅（post_process/scenario_evaluator/metric_topic_map.h）

{proto::ScenarioMetric::kYourMetricName,

{topic::kSomeTopicName, topic::kAnotherTopicName}},

---

第四步：BUILD 文件（两处）

修改 post_process/scenario_evaluator/metrics/BUILD.bazel，新增目标：

cc_library(

name = "your_metric_name_handler",

srcs = ["your_metric_name_handler.cpp"],

hdrs = ["your_metric_name_handler.h"],

deps = [

":metric_handler_base",

"@simulation/simulation/sim_common:topics",

# 按需添加其他依赖

],

alwayslink = 1,  # 必须！否则静态链接会优化掉注册宏

)

修改 post_process/scenario_evaluator/BUILD.bazel，在主目标 deps 中添加：

"//scenario_evaluator/metrics:your_metric_name_handler",

---

文件清单

┌──────┬─────────────────────────────────────────────────────────┐

│ 操作 │                          文件                           │

├──────┼─────────────────────────────────────────────────────────┤

│ 新建 │ simulation_proto/your_metric_name.proto                 │

├──────┼─────────────────────────────────────────────────────────┤

│ 修改 │ simulation_proto/scenario_metric.proto                  │

├──────┼─────────────────────────────────────────────────────────┤

│ 修改 │ simulation_proto/frame_passing_result.proto             │

├──────┼─────────────────────────────────────────────────────────┤

│ 新建 │ scenario_evaluator/metrics/your_metric_name_handler.h   │

├──────┼─────────────────────────────────────────────────────────┤

│ 新建 │ scenario_evaluator/metrics/your_metric_name_handler.cpp │

├──────┼─────────────────────────────────────────────────────────┤

│ 修改 │ scenario_evaluator/metric_topic_map.h                   │

├──────┼─────────────────────────────────────────────────────────┤

│ 修改 │ scenario_evaluator/metrics/BUILD.bazel                  │

├──────┼─────────────────────────────────────────────────────────┤

│ 修改 │ scenario_evaluator/BUILD.bazel                          │

└──────┴─────────────────────────────────────────────────────────┘

▎ 核心机制：REGISTER_METRIC_HANDLER 宏在 .cpp 末尾生成一个静态工厂类并自注册到 MetricHandlerFactory，alwayslink=1

确保静态库链接时不被优化掉。scenario_evaluator.cpp 无需任何修改。

---
*标签：#待整理 #代码 #问题解答 #claude-output*
